# 情绪识别功能使用说明

## 功能简介

本系统新增了情绪识别功能，使输入内容传递至MiniMax后能够被正确识别情绪。该功能支持为部分文稿设置默认情绪值，也允许用户为特定文稿自定义情绪值。

## 情绪参数说明

根据MiniMax T2A API文档，支持的情绪参数包括：

| 情绪值 | 中文描述 | 说明 |
|--------|----------|------|
| `happy` | 高兴 | 欢快愉悦的情绪 |
| `sad` | 悲伤 | 忧郁悲伤的情绪 |
| `angry` | 愤怒 | 愤怒激动的情绪 |
| `fearful` | 害怕 | 恐惧害怕的情绪 |
| `disgusted` | 厌恶 | 厌恶反感的情绪 |
| `surprised` | 惊讶 | 惊讶意外的情绪 |
| `calm` | 中性 | 平静中性的情绪 |
| `fluent` | 生动 | 流畅生动的情绪 |
| `whisper` | 低语 | 低语轻柔的情绪 |

## 使用方法

### 1. 全局默认情绪设置

在语音设置页面，您可以为角色A和角色B分别设置默认情绪：

1. 进入「语音设置」标签页
2. 在角色A的设置区域，找到「选择情绪」下拉菜单
3. 选择您希望的默认情绪，或保持「自动（根据文本内容）」让模型自动识别
4. 同样为角色B设置默认情绪

### 2. 生成播客

当您开始生成播客时，系统会自动将您设置的情绪参数传递给MiniMax API：

1. 进入「生成播客」标签页
2. 确保文稿内容已准备好
3. 点击「开始生成播客」按钮
4. 系统会将每个角色的对话与对应的情绪参数一起发送到MiniMax API

### 3. 情绪参数的传递流程

1. 前端页面通过下拉菜单收集用户选择的情绪
2. 点击生成按钮时，情绪参数通过API请求发送到后端
3. 后端将情绪参数传递给`generate_audio_segments`函数
4. 该函数在合成每个音频片段时，将情绪参数传递给MiniMax API
5. MiniMax API根据情绪参数生成带有对应情绪的语音

## 注意事项

1. 情绪参数仅对以下模型生效：`speech-2.6-hd`, `speech-2.6-turbo`, `speech-02-hd`, `speech-02-turbo`, `speech-01-hd`, `speech-01-turbo`
2. 情绪参数`fluent`和`whisper`仅对`speech-2.6-turbo`和`speech-2.6-hd`模型生效
3. 当情绪参数为空时，MiniMax API会根据文本内容自动识别情绪
4. 建议根据文稿内容选择合适的情绪，以获得更好的合成效果

## 示例使用场景

### 场景1：对话中包含惊讶情绪

文稿：
```
[A]: 你知道吗？我今天中了彩票！
[B]: 真的吗？太令人惊讶了！
```

设置：
- 角色A：默认情绪
- 角色B：惊讶（surprised）

### 场景2：悲伤的独白

文稿：
```
[A]: 今天是她离开的第365天，我依然想念她。
```

设置：
- 角色A：悲伤（sad）

### 场景3：生气的争论

文稿：
```
[A]: 你怎么能这样做？你知道这会带来什么后果吗？
[B]: 我也是迫不得已，你为什么不能理解我？
```

设置：
- 角色A：愤怒（angry）
- 角色B：悲伤（sad）

## 技术实现细节

### 前端实现

在`index.html`中添加了情绪选择下拉菜单：
```html
<div class="mb-3">
    <label for="emotionA" class="form-label">选择情绪</label>
    <select class="form-select" id="emotionA">
        <option value="" selected>自动（根据文本内容）</option>
        <option value="happy">高兴</option>
        <option value="sad">悲伤</option>
        <option value="angry">愤怒</option>
        <option value="fearful">害怕</option>
        <option value="disgusted">厌恶</option>
        <option value="surprised">惊讶</option>
        <option value="calm">中性</option>
        <option value="fluent">生动</option>
        <option value="whisper">低语</option>
    </select>
    <div class="form-text">为空时将根据文本内容自动识别情绪</div>
</div>
```

### 后端实现

1. 在`app.py`中接收情绪参数：
```python
# 获取情绪参数
emotion_a = data.get('emotion_a', '')
emotion_b = data.get('emotion_b', '')
```

2. 创建包含情绪的声音配置：
```python
voice_config = {
    'A': {
        'voice_id': voice_a,
        'emotion': emotion_a
    },
    'B': {
        'voice_id': voice_b,
        'emotion': emotion_b
    }
}
```

3. 在`minimax_client.py`中添加情绪参数支持：
```python
def synthesize_speech(self, text, voice_id, speed=1.0, volume=1.0, pitch=0, emotion="", format="mp3"):
    # ...
    voice_setting = {
        "voice_id": voice_id,
        "speed": speed,
        "vol": volume,
        "pitch": pitch
    }
    
    # 只有当emotion不为空时才添加到voice_setting中
    if emotion:
        voice_setting["emotion"] = emotion
    # ...
```

## 常见问题

### Q: 情绪参数不生效怎么办？
A: 请检查以下几点：
1. 确保使用的模型支持情绪参数
2. 确保情绪参数拼写正确
3. 查看系统日志，确认情绪参数是否正确传递

### Q: 可以为每个句子单独设置情绪吗？
A: 当前版本不支持为每个句子单独设置情绪，只能为整个对话中的角色A和角色B分别设置默认情绪。后续版本将考虑支持更细粒度的情绪设置。

### Q: 自动情绪识别的效果如何？
A: MiniMax API的自动情绪识别效果良好，能够根据文本内容自动判断合适的情绪。如果对自动识别的结果不满意，可以手动指定情绪。

## 更新日志

- 2025-12-20：新增情绪识别功能
- 支持为角色A和角色B分别设置情绪
- 支持9种不同的情绪选择
- 与现有调用方式兼容

## 联系方式

如有任何问题或建议，请联系系统管理员。