# 双人对话播客合成工作流实现计划

## 一、需求分析

用户需要一个本地双人对话播客合成工作流，包含以下功能：

1. 灵感生成
2. 写作输出
3. 生成播客文稿（双人）
4. 优化播客文稿结构
5. 将文稿逐句输入语音合成模型
6. 输出完成后拼接

## 二、技术方案

### 1. 系统架构

* 基于现有Flask Web应用扩展

* 添加新的"播客合成"标签页

* 集成本地语音合成服务

* 支持本地LLM或API调用生成文稿

### 2. 核心功能模块

#### （1）灵感生成模块

* 提供主题输入框

* 支持自动生成相关话题和讨论点

* 可选择不同风格和长度

#### （2）双人对话文稿生成

* 基于输入的主题和灵感，生成结构化的双人对话

* 支持设置对话角色（如主持人A、嘉宾B）

* 可调整对话长度和风格

#### （3）文稿优化模块

* 自动优化对话流畅度和连贯性

* 支持手动编辑和调整

* 提供分段、添加停顿等功能

#### （4）语音合成模块

* 集成本地TTS引擎（如Coqui TTS）

* 支持为不同角色选择不同声音

* 逐句合成语音，生成独立音频文件

#### （5）音频拼接模块

* 将所有合成的音频文件按顺序拼接

* 支持添加背景音乐（可选）

* 生成最终播客音频文件

### 3. 技术选型

| 功能   | 技术方案                  | 优势                |
| ---- | --------------------- | ----------------- |
| 语音合成 | Coqui TTS             | 开源、本地运行、支持多种语言和声音 |
| 文稿生成 | 本地LLM（如Llama 3）或API调用 | 灵活选择，支持离线使用       |
| 音频处理 | pydub                 | 简单易用，支持音频拼接、格式转换  |
| 前端界面 | 基于现有HTML/CSS/JS扩展     | 保持一致的用户体验         |

## 三、实现步骤

### 1. 项目准备

* 安装必要的依赖包：`coqui-tts`、`pydub`、`ffmpeg`

* 确保ffmpeg已安装并添加到系统PATH

### 2. 后端开发

* 添加新的Flask路由：

  * `/podcast/generate-script` - 生成双人对话文稿

  * `/podcast/optimize-script` - 优化文稿结构

  * `/podcast/synthesize-speech` - 合成语音

  * `/podcast/merge-audio` - 拼接音频文件

* 创建语音合成模块：`siliconflow_text_to_speech.py`

* 实现音频拼接功能

### 3. 前端开发

* 在现有HTML中添加"播客合成"标签页

* 设计灵感生成表单

* 实现双人对话文稿编辑器

* 添加语音合成参数设置（声音选择、语速等）

* 提供音频预览和下载功能

### 4. 集成与测试

* 整合所有模块

* 测试完整工作流

* 优化性能和用户体验

## 四、文件结构变化

### 新增文件

* `siliconflow_text_to_speech.py` - 语音合成模块

* `podcast_utils.py` - 播客相关工具函数

* 静态资源目录 `音频/` - 用于存储生成的音频文件

### 修改文件

* `app.py` - 添加播客合成相关路由和功能

## 五、使用流程

1. 用户进入"播客合成"标签页
2. 输入主题，生成灵感和讨论点
3. 生成双人对话文稿
4. 编辑和优化文稿结构
5. 为两个角色选择不同声音
6. 点击"生成播客"按钮
7. 系统自动合成语音并拼接
8. 用户预览和下载最终播客音频

## 六、注意事项

* 确保本地TTS模型有足够的计算资源

* 首次运行可能需要下载模型文件

* 音频合成和拼接过程可能需要较长时间

* 建议使用高质量的TTS模型以获得更好的效果

